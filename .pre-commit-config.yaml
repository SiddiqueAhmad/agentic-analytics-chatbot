repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Use a valid tag. v0.9.1 is a recent stable version.
    rev: v0.14.10
    hooks:
      # Run the linter.
      - id: ruff
        args: [ --fix ]
      # Run the formatter.
      - id: ruff-format      